machine:
  python:
    version: 3.5.1
  services:
    - postgresql

database:
  override:
    - psql -c "CREATE ROLE app PASSWORD 'password' SUPERUSER CREATEDB INHERIT LOGIN"
    - python manage.py makemigrations genes
    - python manage.py makemigrations organisms